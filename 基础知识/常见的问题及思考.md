gy

- 服务端是怎么存储用户密码的，明文还是密文，如何加密。 
- 如果一个网站的用户信息泄露，被用来和其他网站撞库的话，会不会成功，为什么

## CSS
盒子模型， background的覆盖范围有没有包括border， margin合并， 怎么避免父子元素的margin合并
baseline
position的几种类型，relative的元素是否会影响其他元素
CSS优先级， 权重

S
for循环判断输出， 怎么实现输出 0-n， -> let 闭包
this的指向
异步处理，微任务和宏任务任务， 为什么要区分宏任务和微任务
js有哪些作用域， -> 全局，函数，块级
怎么形成块级作用域
js闭包，and 闭包为何能访问外部函数变量，外部函数的活动对象为什么不会被销毁

HTML
输入url的过程
怎么优化js阻塞DOM树解析

HTTP
http的类型
PUT和POST有什么区别
https和http的区别
https数据传输怎么保证安全性
https如何保证服务端和浏览器端发送的数据只有对方可以解密

cookie怎么样不被恶意脚本访问
用户登录信息怎么保存
多服务器怎么同步用户登录信息
TOKEN由哪些部分组成
怎么实现跨域
CORS需要设置哪些字段
CORS的请求和正常的HTTP请求有什么区别

ly

一、yilian

1、父组件下有N个嵌套的子组件，最里面那个子怎么和父通信

2、什么情况下用到防抖、节流，列出来。自己会实现这两个函数吗

3、口述懒加载图片的实现

4、从url到dom, tcp连接说清楚

5、怎么实现输入home页的链接，在没登录的状态下跳转到login页（答：路由拦截）

6、你平时怎么实现异步的？promise怎么用，它有什么缺点。es6你常用哪些特性。记得有个问题回答了Promise.all()

7、vue的双向绑定原理

8、跨域的方法列一些。即时通讯了解多少

9、http2.0了解多少

10、闭包会穿插在你回答业务问题时问

二 hlg
1、正则基础的要会写，+ *什么之类的会问

2、如果简历中写了移动端有做过，会问你：

怎么解决1px误差的问题
设计稿750px,怎么适配各种手机屏幕
三个div怎么实现水平垂直居中
除了BFC，你还知道什么其他的FC
怎么画三角形，你有多少种方式
scss中的函数你怎么用的
除了变量你还用到scss的哪些特性
各种图片格式的应用场景
3、路由的原理

4、vue的双向绑定原理

5、简述webpack的原理

6、自己会配webpack配置吗

7、跨域方式

8、this的指向

9、怎么调用一个函数。（回答了立即调用、obj.fn（）、定时器调用、面试官提醒还有 new Fn()这种方式）

10、原型链这块会出了一个题让口述回答

11、详述mtjs的原理

12、糖饼主要是让我手动演示自己做过的项目，会提问题，例如优化了哪些地方，哪些地方是怎么实现的等

13、vue怎么对seo友好

14、安全、防攻击相关

三、野生公司
1、postMessage这种跨域方式

2、css怎么做性能优化

减少重绘和回流
1、用visibility代码display:none;
2、使用 transform 替代 top
3、不要使用 table 布局
4、动画实现的速度的选择，动画速度越快，回流次数越多，也可以选择使用 requestAnimationFrame
5、CSS 选择符从右往左匹配查找，避免节点层级过多
6、将频繁重绘或者回流的节点设置为图层，图层能够阻止该节点的渲染行为影响别的节点。比如对于 video 标签来说，浏览器会自动将该节点变为图层。
7、设置节点为图层的方式有很多，我们可以通过以下几个常用属性可以生成新图层

will-change
video、iframe 标签
3、vue你觉得不合理的地方

4、node.js的基本模块

5、content-type：内容类型

- application/x-www-form-urlencoded
数据发送出去后，需要接收的服务端解析成功，一般服务端会根据content-type字段来获取参数是怎么编码的，然后对应去解码。
 
在最早的http post请求中，只支持application/x-www-form-urlencoded，参数都是通过浏览器的url传递

- application/json